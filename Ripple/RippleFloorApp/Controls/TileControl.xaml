<UserControl x:Class="RippleFloorApp.Controls.TileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="TileID" Background="#FF8C00" Style="{StaticResource GridStyle}">
        <Border Style="{StaticResource BorderStyle}">
            <Grid>
                <TextBlock x:Name="TileIDLabel" Text="ID" Style="{StaticResource TextContentStyle}" FontSize="32" />
                <Grid x:Name="InnerContentTileID" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
