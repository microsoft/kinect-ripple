<UserControl x:Class="RippleEditor.Utilities.TileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Name="TileID" >
    <Border Style="{StaticResource BorderStyle}">
            <Button x:Name="TileIDButton" Background="#000000">       
            <Grid x:Name="InnerTileID">
                <TextBlock x:Name="TileIDLabel" Text="ID" Style="{StaticResource TextContentStyle}">
                    <TextBlock.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </TextBlock.RenderTransform>
                </TextBlock>
                <Grid x:Name="InnerContentTileID" />
            </Grid>
    </Button>
    </Border>
    </Grid>
</UserControl>
